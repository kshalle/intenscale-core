instname = riscv-tests
instbasedir = $(UCB_VLSI_HOME)/install

# Original set
tests = median qsort rsort towers vvadd multiply mm dhrystone spmv \
	mt-vvadd mt-matmul pmp \
# By intensivate
tests += intMul

include ../Makefrag-baremetal

